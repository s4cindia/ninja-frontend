# E2E Test Environment Configuration
# Copy this file to .env.test and configure for your environment

# Mock API Mode (recommended for CI and local development)
# When true, tests use mock API responses - no real backend needed
MOCK_API=true

# Test User Credentials
# Only required when MOCK_API=false (testing against real staging)
# TEST_USER_EMAIL=your-test-user@example.com
# TEST_USER_PASSWORD=your-test-password

# Playwright Base URL
# Default: http://localhost:5000 (local dev server)
# For staging: https://dhi5xqbewozlg.cloudfront.net
PLAYWRIGHT_BASE_URL=http://localhost:5000

# CI Environment (automatically set by GitHub Actions)
# CI=true
